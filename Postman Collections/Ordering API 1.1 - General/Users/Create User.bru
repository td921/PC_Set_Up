meta {
  name: Create User
  type: http
  seq: 9
}

post {
  url: {{url}}/users/create?key={{apikey}}
  body: json
  auth: none
}

params:query {
  key: {{apikey}}
}

headers {
  X-Forwarded-For: 24.24.24.24
}

body:json {
  {
      "firstname": "John",
      "lastname": "Doe",
      "emailaddress": "tina.dinh@olo.com",
      "password": "testing123!"
  }
}

tests {
  var data = JSON.parse(responseBody);
  
  // postman.setEnvironmentVariable("authtoken", data.authtoken);
}
