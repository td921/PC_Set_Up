meta {
  name: Metric
  type: http
  seq: 3
}

post {
  url: {{url}}/metrics?key={{defaultAdvanceKey}}
  body: json
  auth: none
}

params:query {
  key: {{defaultAdvanceKey}}
}

body:json {
  {
      "source": "testing"
  }
}
